{
	"presets": [<% if ( esModules ) { %>
		"babel-preset-niksy/next",<% } %><% if ( browserModule ) { %>
		"babel-preset-niksy"<% } else { %>
		["babel-preset-niksy", {
			"@babel/preset-env": {
				"targets": {
					"node": "<%= nodeEngineVersion %>"
				}
			}
		}]<% } %>
	]<% if ( browserModule ) { %>,
	"plugins": [
		"@babel/plugin-transform-object-assign"
	]<% } %><% if ( automatedTests && codeCoverage && !browserModule ) { %>,
	"env": {
		"test": {
			"plugins": [
				"babel-plugin-istanbul"
			]
		}
	}<% } %><% if ( automatedTests && browserModule && !sassModule ) { %>,
	"env": {
		"test": {
			"presets": [
				["babel-preset-niksy", {
					"@babel/preset-env": {
						"useBuiltIns": "usage"
					}
				}]
			],
			"plugins": [
				["@babel/plugin-transform-runtime", {
					"corejs": false,
					"helpers": true,
					"regenerator": false,
					"useESModules": true
				}]
			]
		}
	}<% } %><% if ( integrationTests ) { %>,
	"overrides": [
		{
			"test": "./test/integration",
			"presets": [
				["babel-preset-niksy", {
					"@babel/preset-env": {
						"targets": {
							"node": "<%= nodeEngineVersion %>"
						}
					}
				}]
			]
		}
	]<% } %>
}
