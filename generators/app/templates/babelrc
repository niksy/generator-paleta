{
	"presets": [<% if ( esModules ) { %>
		"niksy/next",<% } %>
		"niksy"
	]<% if ( browserModule ) { %>,
	"plugins": [
		"transform-object-assign"
	]<% } %><% if ( automatedTests && codeCoverage && !browserModule ) { %>,
	"env": {
		"test": {
			"plugins": [
				"istanbul"
			]
		}
	}<% } %>
}
