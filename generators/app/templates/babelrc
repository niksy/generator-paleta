{
	"presets": [<% if ( esModules ) { %>
		"babel-preset-niksy/next",<% } %>
		["babel-preset-niksy", {
			"targets": {
				"node": "<%= nodeEngineVersion %>"<% if ( browserModule ) { %>,
				"browsers": [<% for ( var i = 0, browserVersionLength = browserVersion.length; i < browserVersionLength; i++ ) { %>
					"<%- browserVersion[i] %>"<% if ( i !== browserVersionLength-1 ) { %>,<% } %><% } %>
				]<% } %>
			}
		}]
	]<% if ( browserModule ) { %>,
	"plugins": [
		"@babel/plugin-transform-object-assign"
	]<% } %><% if ( automatedTests && codeCoverage && !browserModule ) { %>,
	"env": {
		"test": {
			"plugins": [
				"babel-plugin-istanbul"
			]
		}
	}<% } %>
}
