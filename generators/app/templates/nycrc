{
	"statements": 80,
	"lines": 0,
	"reporter": [
		"html"<% if ( codeCoverageService ) { %>,
		"lcov"<% } %>,
		"text"
	]<% if ( transpile && codeCoverage ) { %>,
	"sourceMap": false,
	"instrument": false<% } %>
}
