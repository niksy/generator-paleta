.DS_Store
node_modules/
package-lock.json
yarn.lock
npm-debug.log<% if ( (automatedTests && browserModule && !sassModule) || manualTests || integrationTests ) { %>
test-dist/<% } %><% if ( automatedTests && codeCoverage ) { %><% if ( codeCoverageTool === 'nyc' && !browserModule ) { %>
.nyc_output/<% } %>
coverage/<% } %><% if ( (automatedTests && browserModule && !sassModule) && integrationTests ) { %>
errorShots/<% } %><% if ( transpile ) { %><% if ( complexTranspile ) { %>
index.js
lib/*
!src/*<% } else { %>
dist/<% } %><% } %>
