language: node_js
node_js:
  - '4'<% if ( !onlyNodeLts ) { %>
  - '0.12'
  - '0.10'<% } %>
branches:
  only:
    - /^v[0-9]/<% if ( codeCoverageService ) { %>
after_success:
  - npm run posttest:ci<% } %>
